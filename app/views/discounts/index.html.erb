<h1><%= @merchant.name %>'s Discounts</h1>
<%= link_to 'Create New Discount', new_merchant_discount_path(@merchant) %>


<% @merchant.discounts.each do |discount| %>
   <p>Quantity Threshhold: <%= discount.quantity %></p>
   <p>Percentage Off: <%= discount.percentage %>%</p>
   <p><%= link_to "See more information about Discount", merchant_discount_path(@merchant, discount) %></p>
   <p><%= link_to "Delete Discount", merchant_discount_path(@merchant, discount), method: :delete %></p>
<% end %>
